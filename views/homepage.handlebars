<article class="post">
  <div class="card">
    <div class="card-header">
      <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
      <p>Posted by {{user.username}} on {{post.created_at}}</p>
    </div>
    <p class="card-body">{{content}}</p>
  </div>
</article> 